fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=13.1
numpy>=2.0
certifi
# health_bridge imports ml/predict.py, and vision_bridge runs YOLO: install ml/requirements.txt
# into the same environment (the team runs everything from ml/venv).
